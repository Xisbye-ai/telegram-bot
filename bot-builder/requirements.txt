# Сервер и интерфейс
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5

# Экран и картинки
numpy>=1.24
opencv-python>=4.8
mss>=9.0
pillow>=10.0

# Мышь и клавиатура
pyautogui>=0.9.54
pyperclip>=1.8

# Нейросеть (обучение и блок «Найти объект»).
# Если не нужна — можно удалить эту строку, остальное будет работать.
torch>=2.1
